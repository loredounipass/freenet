// Deprecated: `processMultimediaFile` removed to avoid duplicate processing logic.
// All multimedia processing should happen in the worker (`MultimediaProcessor`).
// Keeping this file as a placeholder to avoid accidental imports elsewhere.

export default {};
